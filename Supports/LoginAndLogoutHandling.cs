using System;
using System.Windows;
using System.Windows.Input;

namespace WPFOgloszenia.Supports {
    public static class LoginAndLogoutHandling {
        public static readonly MainWindow? window = Application.Current.MainWindow as MainWindow;
        public static void LoginChanges() {
            if (window is not null) {
                
            }
        }
        public static void Logout() {
            if (window is not null) {
                
            }
        }
        
    }
}
